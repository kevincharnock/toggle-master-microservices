apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: default
  labels:
    app: analytics-service

spec:
  type: ClusterIP

  ports:
    - name: http
      port: 8005
      targetPort: 8005
      protocol: TCP

  selector:
    app: analytics-service
