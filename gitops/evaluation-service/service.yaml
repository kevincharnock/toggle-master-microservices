apiVersion: v1
kind: Service
metadata:
  name: evaluation-service
  namespace: default
  labels:
    app: evaluation-service

spec:
  type: ClusterIP

  ports:
    - name: http
      port: 8004
      targetPort: 8004
      protocol: TCP

  selector:
    app: evaluation-service
